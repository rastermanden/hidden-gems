{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "hidden-gems",
      "runtimeExecutable": "npx",
      "runtimeArgs": ["browser-sync", "start", "--server", "--port", "8000", "--files", "**", "--no-open", "--watchOptions.usePolling=true", "--watchOptions.interval=500"],
      "port": 8000,
      "autoPort": false
    }
  ]
}
