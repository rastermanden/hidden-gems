{
  "name": "hidden-gems",
  "version": "1.0.0",
  "scripts": {
    "start": "npx browser-sync start --server --port 8000 --files '**' --no-open --watchOptions.usePolling=true --watchOptions.interval=500"
  }
}
